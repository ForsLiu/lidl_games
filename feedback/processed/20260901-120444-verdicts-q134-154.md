[verdicts] Owner verdicts for pending entries Q134–Q154

General rule: every pending entry not named below is APPROVED as its
chosen default. Record each verdict in its entry and in the Verdict log,
then apply the overrides and orders.

Q134: approved — the auto-allocated Constellation applies in normal play,
not only dev.
Q135: approved as built. A full owner spec for the Training Grounds
(five dummy variants with live per-dummy readouts, practice tools incl.
TD/VS context switch) arrives as its own feedback file; treat it as an
extension of fb019, not a redesign.
Q136: approved, all four calls.
Q137: approved (ratio 0.5, one level per sweep, overflow to gold).
Q138: OVERRIDE — balance tooling must measure what players play. Default
tools/sim.ts, tools/sweep.ts, tools/handoff-metrics.ts and every gate
measurement to the same allocation live runs use (TREE_AUTO_MAX = full
tree); keep an explicit `--tree none` / partial option for deliberate
scenarios. Re-measure G8/G23/G14/G1 after switching and record the deltas
before p10r's retune continues.
Q139: approved, all calls, including the execute denying lifesteal and
Time Lock radius 3 ⚖.
Q140, Q141: approved.
Q142: ORDER (small polish item): make the Constellation screen format
cdr/leech via STAT_KIND / info-format's modIsPct, one deliberate change,
so every surface agrees.
Q143: approved; p7d/b037 own the pipeline retirement.
Q144: (1) OVERRIDE — the owner's standing instruction is no rank caps on
stat boons and Type Mastery cards; amend SPEC-FINAL §6.3 to say so (skill
cards keep their listed caps) and re-apply fb011 to the new pool.
(2) approved — second_wind dropped, engine code kept. (3) approved as ⚖
placeholders for the nine class lines.
Q145: approved.
Q146: approved for now; ORDER at the P10 content/balance pass: give the
13 emptied small nodes flat additive effects only (e.g. +5 starting gold
each ⚖) and the two notables flat effects (e.g. +25 starting gold; one
free tower upgrade step at run start ⚖) — additive, never multiplicative,
so the compounding concern does not apply; balance-analyst re-checks
G1/G6/G14 after.
Q147, Q148: approved (the 8–12 band is for class quests; Core unlocks are
their own closed set of 4).
Q149: OVERRIDE — "finish a run" for Vampire Heart's unlock means the run
ends with the Core still standing at ≤25% HP (victory or defeat_warden);
a Core-death defeat does not unlock it. Update the quest + tests.
Q150: approved as v1. ORDER (normal priority, after the current owner
batch): per-field editors for the collections the owner tunes most —
towers, classes, cores, waves — on top of the JSON-document editor.
Q151: OVERRIDE — the 20 s idle auto-resolve applies only to unattended
runs (RunConfig with a bot policy or headless/sim runs). A human-driven
UI run never auto-resolves a level-up unless auto-pick is on; it waits.
Q152: approved — synthesized cues stand until the owner supplies media.
Q153: approved.
Q154: approved reading. ORDER: add to BALANCE.md a "play matters" band —
a never-moving character's T1 win rate ≤60% ⚖ (bot `no-move`) — to be met
by the P10 re-tune after the owner's enemy-HP/attack-speed order lands;
log the measured number either way.

Additional ORDER (bug check): PROGRESS.md flags that kite/rush/walloff T3
win rates sit suspiciously close to T1. Verify tools/sweep.ts's `--tier`
applies the tier scalars to every policy path; fix with a regression test
if it does not.

Record every verdict, apply the overrides and orders, then continue the
backlog from the top.
