# HANDOFF.md — Stonewake v1.0 engineering state report

For whoever picks up P10's balance re-baseline and the 1.0 sign-off. Describes
what the code **actually does** against **SPEC-FINAL.md** (the only current
spec — SPEC.md/SPEC-V2.md/SPEC-V3.md are superseded history), not what any
prior spec draft said. Every number here was measured or read out of `/data`
on 2026-09-01 at commit `31fb74e` (regenerated by BACKLOG p10n; the previous
version, dated 2026-08-30 at `cc4ee58`, predates the `p10j`-`p10l` balance
pass that closed gates G1 and G13, `p10m`'s G8/G14/G23 re-measurement, and
`b070`/`b071`'s Core-fingerprint and VS-damage-share fixes — none of that
work is reflected in it; see PROGRESS.md for the full session-by-session
history).

Regenerate the measured sections with:

```
npx tsx tools/handoff-metrics.ts     # sweep table, boon picks
npx tsx tools/a4probe.ts             # per-tower solo viability (G13's a4 half)
npx tsx tools/a5probe.ts             # VS damage share, top-10 builds (G13)
npx tsx tools/content-census.ts      # §13 content totals
npx tsx tools/gate-audit.ts          # §14 gate → test-file coverage map
```

---

## 1. Implemented systems

### Simulation core (`src/sim/`)
| System | How it actually works |
|---|---|
| Loop | Fixed 60 Hz. `Run.step(TickInput)` advances exactly one tick; no wall-clock anywhere in `/src/sim`. |
| Determinism | Run = `RunConfig` + input log. End state is FNV-1a hashed over quantised positions/HP/RNG state, including class actives, VS boon picks and Core choice. `RunConfig.contentHash` is a function of `/data`'s own authored bytes (`Content.raw`, fixed at b044) so it fails a replay loudly against edited `/data` (G2), without false-failing on a loader/schema change alone. |
| RNG | mulberry32, five independent streams (`waves`, `spawns`, `drops`, `offers`, `ai`) seeded by FNV-1a of the stream name + run seed. |
| Math | `dsin`/`dcos`/`datan2` are polynomial; the sim never calls native trig. |
| Grid | 36×20 tiles, 3 gates (W/N/E), Core 2×2 east-center. Integer-cost Dijkstra produces a ground field and a ghost field toward the Core/Warden. |
| Sealing | Legal (§10): a structure tile is a high-cost passable tile (`breach.base + perEhp × effective max HP`), so a sealed board routes the cheapest breach and chews structures on the way; an open path is always cheaper. Fliers/Burrowers/Wraiths keep their bypasses. Gate G7. |
| Run structure | **The real §1.1 shape**: 18 TD waves + 6 VS waves, TD×3→VS repeating, `RunConfig.cycles` (default 6) gates how many TD/VS blocks a run has. TD wave 18 ends with the Gatebreaker miniboss; the final VS wave (block 6) ends with the Warden-Eater. Multi-summon stacks up to 3 TD waves (`maxStackedWaves`); every TD wave cleared pays a flat `20 + 10×wave` gold, early or not (fb009). Leak coupling: each TD leak adds `2×` its spawn cost to the next VS wave's budget. **Build phase 15 s** (cut from 20 s at `p10l` — pure dead-time removal, does not touch any bot's TD economy or combat difficulty); VS wave 75 s (final block runs to the boss kill). Gate G6. |
| Spatial index | One pooled bucket array per tile, rebuilt each tick. |
| Enemies | 20 defs (§9), trait behaviour dispatched off a cached bitmask. |
| Towers | 10 defs (§5), 7 attack kinds (`single`/`pierce`/`cone`/`aura`/`chain`/`lob`/`poison`). Flat per-step upgrade cost (not growing); sell refunds 50% of everything spent, in every phase. Gate G20 (every §5 milestone special measurably changes the attack it names). |
| Cores | 5 defs (§5.5): Stone Heart (default), Carnivorous Plant, Vampire Heart, Corpse, Time — chosen at run start, hashed into `RunConfig`, each with its own TD and VS effect and an upgrade track bought at the Core. Gates G21 (unit-tested numbers)/G22 (fingerprint shift, `corpse`'s seed-2 regression fixed at `b071` — `storeRatio` 0.02→0.03)/G23 (T1 win rate — see §4, currently red the other direction). |
| VS inheritance | §6.1's formula, verbatim: a built tower type's VS attack damage is the average of its instances' damage (by upgrade level), boosted +10% per extra tower of that type; special effects and attack speed come from the highest-upgrade instance. Directional wielded attacks (`single`/`pierce`/`lob`/`poison`) also carry a small engine-side crowd allowance (`wieldSplash`, `src/sim/vswield.ts`, landed at `p10j`) so no single tower type's VS share can structurally dominate. VS attacks count as character attacks (lifesteal, on-attack passives apply). Gate G3. |
| Classes | 12 defs (§4): archetype bands + Passive + Active1 (Q) + Active2 (E) + always-on tower passive, per class. All actives are sim Commands. Swordsman/Archer/Engineer free; the other 9 unlock via quests. |
| Damage & stacking | 6 damage types + 2 statuses (§3, `data/damagetypes.json`), armor +99 cap / −100 floor (`reduction = clamp(a,-100,99)/100`), DoTs ignore armor, Burning shreds armor and stacks per-application under the shared 50-stacks/enemy cap. Stacking: different sources multiply, same-source ranks add (§2). Gates G4/G5. |
| Equipment | 12 items (§7, one per weapon/armor/shoes/ring/necklace/bracelet slot), stash click-to-swap, `N` TD waves cleared → `N` equipment at run end. Gate G12. |
| VS level-up pool | §6.3: 1-of-3 cards per level, 1 free reroll; stat boons (uncapped rank, fb011), Type Mastery per built tower type, 3 skill cards per class. |
| Rewards & meta | TD wave cleared → equipment; VS wave cleared → skill point; 120-node Constellation (adjacency-gated, connectivity-preserving refunds); 24-slot stash; 10 unlock quests (§8.4); no currency-granting quests; Orbs/relics/Ember retired outright (G12's "orbs nowhere"). |
| Boss | The Warden-Eater: 3 phases at 100/66/30% HP (telegraphed charges shattering terrain, Wraith summons + slam rings, a closing fire-ring enrage), plus an independent damage-taken pacing ramp (`PACING_START`/`PACING_INTERVAL`/`PACING_VULNERABILITY_PER_STACK`, `src/sim/boss.ts`, landed at `p10k`) that shortens a long fight without moving the win/loss outcome. Gatebreaker (TD wave 18 miniboss, 2× vs structures). |
| Loot | Even-weight equipment drops per cleared TD wave. |

### Outside the sim
| System | How it actually works |
|---|---|
| Bots (`src/bots/`) | `idle`, `kite`, `turtle`, `hybrid`, `no-move`, `maxbuild`, `rush`, `walloff`, `greedy`, `greedless`, plus `sealed` (G7) and the `single:<tower>` builder used by `tools/a4probe.ts`/`a5probe.ts`. None issues class actives on its own — gate measurements that need a kit script it locally (`tests/p6e-class-diversity.test.ts`, `tests/p-core-f-gates.test.ts`). |
| Meta (`src/meta/`) | Skill points → 120-node Constellation tree; 24-slot equipment stash; 10 quests; localStorage save with versioned migration and corrupt-save repair. |
| Renderer (`src/render/`) | Canvas, backed at device pixel ratio, reads sim state only. Hit flash, damage numbers (per-damage-type color/font, `data/damagetypes.json`), shake, boss telegraphs, arena fire, the DoT-segment HP bar, a 2 s TD↔VS transition sweep (`w.fx`-driven, outside `hashWorld`). |
| SFX | Every gameplay event maps to a rate-limited synthesised cue behind an `AudioSink` seam; asset pass still pending (BETA). |
| UI (`src/ui/`) | Hub (class / Core / tier draft / Constellation / stash / settings) → run (canvas + HUD + modals, DPS summary panel, tower/weapon lineage panels, character panel with per-source stat breakdown) → results → Hub. Esc pauses and offers Abandon Run (confirm). |
| Headless CLI | `npm run sim` prints a JSON report per run: `simMs`, damage by source, per-wave telemetry. |
| Fast-forward | 1/2/3/10/50×; the renderer may skip frames at 10× and above but the sim steps the same fixed ticks, so a fast-forwarded run is bit-identical to the same run at 1×. |
| Practice runs | Kill all, +gold, +XP, heal, invulnerable, skip wave, +1 minute, summon boss — ordinary Commands gated on `RunConfig.practice`; a run that used one banks nothing. |
| Dev profile / god mode | `data/dev.json` unlocks every class/Core/map, memory-only (never persisted), production build has it off and god mode unreachable (G16). |
| Codex & Tuner | In-game wiki of every `/data` entity; in dev mode every number/wave is editable and saved to the real files via the dev-server, content-hashed into `RunConfig` (G2's tuner-edited-content clause); prod is read-only + export/import, with no save endpoint (G15). |
| Test seeding | Settings can fill an account with equipment/skill points, or wipe it, deterministically. |

---

## 2. Content totals at 1.0 (SPEC-FINAL §13)

`npx tsx tools/content-census.ts` — **10/10 categories met**, unchanged since `p10i`:

| Category | Actual | Target | Met |
|---|---|---|---|
| Classes | 12 | 12 | yes |
| Towers | 10 | 10 | yes |
| Equipment | 12 | 12+ | yes |
| Damage types + statuses | 6+2 | 6+2 | yes |
| Enemies | 20 | 20 | yes |
| Waves | 18+6 (24) | 18+6 (24) | yes |
| Tree nodes (excl. start) | 120 | 120 | yes |
| Quests | 10 | 8–12 | yes |
| Map tiers | T1–T5 | T1–T5 | yes |
| Bosses | 2 | 2 | yes |

Content is complete. **The open work at 1.0 is entirely balance, not scope** —
see §4 and §6 below.

---

## 3. Current `/data` tuning

### Towers (`data/towers.json`)
Flat per-step upgrade cost, does not grow; each step is +10% HP/Attack/Defense
unless a milestone special replaces it (§5). Sell refunds 50% of spend.

| Tower | Cost | Attack (kind, dmg/dps, interval, range) | HP | Def | Steps | Step cost | VS special |
|---|---|---|---|---|---|---|---|
| palisade | 10 | — (obstacle) | 300 | 0 | 0 | — | none |
| arrow_spire | 50 | single 10, 0.5 s, r5 | 120 | 10 | 5 | 20 | none |
| ballista | 90 | pierce 18, 1.0 s, r8 | 140 | 10 | 10 | 18 | none |
| ember_brazier | 70 | cone 2.7 dps, 0.25 s, r3.5 | 110 | 5 | 4 | 35 | Burning enemies explode on death |
| frost_obelisk | 70 | aura **18** dps, 1.0 s, r3 | 130 | 10 | 9 | 16 | ice aura follows the character in VS |
| tesla_coil | 120 | chain 29, 1.0 s, r5 | 110 | 10 | 3 | 40 | electric wire grid links towers |
| mortar | 130 | lob 95, 2.2 s, r10 (min 3) | 120 | 5 | 3 | 26 | none |
| venom_spore | 75 | poison 38, 1.0 s, r4.5 | 110 | 5 | 4 | 38 | character leaves a poison trail (VS) |
| beacon_totem | 60 | — aura r3, towers +atk spd | 100 | 5 | 2 | 60 | beacon haste radius around the character (VS) |
| harvest_sprout | 40 | — +gold/wave | 90 | 0 | 2 | 40 | emits an XP gem periodically (VS) |

`frost_obelisk.attack.damage` was cut 19→18 at `b071` (a 5.3% trim) to
re-close G13's 35% VS-damage-share cap after `p10l`'s `buildPhaseSeconds`
20→15 pushed it back over — see §4.

### Equipment (`data/equipment.json`)
12 items across the 6 slots (weapon/armor/shoes/ring/necklace/bracelet), as
SPEC-FINAL §7's owner table verbatim — no deviation.

### Damage types & statuses (`data/damagetypes.json`)
Matches §3 verbatim, including the owner-intent flip landed at **p10a**:
Burning now stacks **per application** (was refresh-strongest at ≤1 stack),
sharing the 50-stack/enemy cap with Bleeding; `immuneTrait` is data-driven
(`burnImmune`), not hardcoded to Burning specifically (p10b).

### Act I / TD (`data/waves.json`)
`hpScalePerWave` **1.22** · **build phase 15 s** (cut from 20 s at `p10l`) ·
wave-clear reward `20 + 10×wave` (fb009) · start gold 250 · Core 500 HP ·
spawn interval 1.02 s · `maxStackedWaves` 3. 18 authored TD wave rows on the
real §1.1 shape (p8a).

### Act II / VS (`data/spawns.json`)
Budget `150 × 1.21^minute`, warm-up over 75 s (§1.1) · `hpOverlay` 0.6 ·
`hpScalePerMinute` 1.10 · `speedOverlay` 1.20 · alive cap 350 · elite every
90 s · rifts at 180/360/540 s (×3 budget) · `bossTimeSeconds` 181 (unchanged
since `p10d`) · leak-budget multiplier 2× (§1.1's leak coupling).

### Warden (`data/warden.json`)
100 HP · 0.5 regen/s · 0 base armor · `reduction = clamp(a,-100,99)/100` ·
armor cap +99 / floor −100 (§2, owner-vetoable per §17) · 4.5 tiles/s ·
pickup 1.5 · dash 4 tiles / 3 s cd / 0.15 s i-frames · CDR cap 40% ·
Heartstone 1.0 HP/s in r3 · manual attack disabled in Act II (VS wielding
replaces it, §6.1).

### Boss (`data/enemies.json`)
Warden-Eater: 10,000 HP (unchanged since `p10d`), 3 phases, spawns at 3:01,
plus `p10k`'s independent damage-taken pacing ramp (§1 above). Gatebreaker:
3,000 HP, TD wave 18, 2× damage vs structures.

### Core (`data/cores.json`)
`corpse.storeRatio` 0.02→0.03 at `b070` (Corpse-Core-only, ⚖ per SPEC-FINAL
§5.5) to restore G22's seed-2 fingerprint margin after `p10l`'s pacing pass
narrowed it under the 0.10 floor.

### Meta
120-node Constellation (3 branches: tower/character/economy), adjacency
allocation, respec 1 point/node. 10 quests (`data/quests.json`), unlocks
only, no currency reward. No Ember, no Orbs, no relic affixes — skill points
are the tree's only currency (§8).

---

## 4. Measured metrics

### Gate coverage (`npx tsx tools/gate-audit.ts`)
23 gates parsed live from SPEC-FINAL §14. **Caveat, still open (BACKLOG
p10o)**: the tool's own `GATE_COVERAGE`/`KNOWN_HOLES` maps (and
`tests/q10-gate-audit.test.ts`'s pin) still print **G8** and **G15** as
`hole` even though both gained live test coverage at `p6e`/`p9c` — a
stale-tooling gap, not a stale spec fact, unfixed as of this regeneration.

The honest state, gate by gate, from the live test suite (not the stale
tool), re-measured this session:

| Gate | State |
|---|---|
| G1 (30–36 min mean) | **Green in full.** `tests/p10d-run-length.test.ts`: mean victorious run **35.20 min**, 21/24 wins (88%) — comfortably inside the band. Closed via `buildPhaseSeconds` 20→15 (`p10l`, pure dead-time removal) plus an independent boss-pacing ramp (`p10k`); re-verified unaffected by `b071`'s G13 fix. |
| G2 (determinism) | **Green in full**, including tuner-edited content and fast-forward (p9f), and `contentHash`'s authored-bytes fix (b044). |
| G3 (VS inheritance) | **Green**, incl. §6.1's worked example verbatim. |
| G4 (armor edges + Burning shred) | **Green**, proven live through a real Ember Brazier build (p10g). |
| G5 (stacking) | **Green.** |
| G6 (interleave/reward) | **Green in full**, incl. fb009's fixed wave reward. |
| G7 (sealing) | **Green in full.** |
| G8 (class win rate 35–70%, ≥9/12 diversity) | **Red, measured, `.skip`-ed — an over-ceiling failure, not the old under-floor one.** `tests/p6e-class-diversity.test.ts`, re-run against HEAD at `p10m`: of 11 individually-tested classes (Time Lord, the 12th class added at `fb013`, is folded into the diversity roster but has never had its own win-rate case written), 9 clear 91.7–100% (past the 70% ceiling) and only `necromancer` stays under the 35% floor at 4/12 (33%) — none land inside the band. Diversity is unmoved: top damage source is still only 2 distinct keys (`ballista`/`spreading_plague`) across all 12 classes, far under the ≥9/12 the gate asks for. Win-rate retune filed as **p10r**; diversity has no item scoped to it yet. |
| G9 (Swordsman/Plaguebringer combos) | **Green.** |
| G10 (Archer charge) | **Green.** |
| G11 (Stormcaller chain ≤×3.6) | **Green.** |
| G12 (rewards, orbs nowhere) | **Green in full.** |
| G13 (no tower >35% VS share; solo T1-viable/T3-not) | **Partial — corrected downward this regeneration.** The 35%-share cap is **green in full**: `tests/p10c-weapon-share.test.ts` measures `frost_obelisk` at **25.9%** (`tools/a5probe.ts`), 9.1 points of margin (`b071`). The solo-viability clause is **not** green as the prior HANDOFF claimed: `tests/a4-single-type.test.ts` currently fails **4 of its 16 live (non-`.skip`) assertions** — `ember_brazier` (3/5, not 5/5) and `tesla_coil` (2/5, not 5/5) at T1, `mortar` and `venom_spore` (1/5 each, not 0/5) at T3. `b071`'s own PROGRESS entry found and named this drift while fixing the share cap, confirmed it pre-existing and unrelated to that fix via `git stash`, and flagged it as "worth its own backlog item, not filed here" — but no such item existed until this regeneration filed **b072**. |
| G14 (boss 20 seeds, 60–<100% win) | **Green in full.** `tests/boss.test.ts`, re-run at `p10m`: **18/20 (90%)**, inside `[60%, 100%)` with real headroom on both sides. Closed by the same `p10j`-`p10l` pacing pass that closed G1/G13. |
| G15 (Tuner round-trip) | **Green** (`p9c`; the gate-audit tool's own map is stale here, see caveat above). |
| G16 (dev profile / prod god-mode-off) | **Green.** |
| G17 (perf budget, 60fps benchmark, soak) | **Green in full** (all three clauses; `p10e` closed the per-simulated-minute budget). |
| G18 (UI flows, save round-trip) | **Green.** |
| G19 (liveness: sealed + open + multi-summon all win) | **Green in full** (`p10f`). |
| G20 (milestone specials) | **Green.** |
| G21 (Core numbers, unit-tested) | **Green in full.** |
| G22 (Core fingerprint shift ≥0.10) | **Green in full.** `corpse` vs Stone Heart seed-2 regression (fingerprint 0.080, found by `p10m`'s re-measurement) fixed at `b071` — now 0.272 (seed 1) / 0.266 (seed 2); all 8 cases green, 0 skips. |
| G23 (every Core 35–70% win rate) | **Red for all 5, measured, `.skip`-ed — an over-ceiling failure, not the old under-floor one.** `tests/p-core-f-gates.test.ts`, re-run at `p10m`: `carnivorous_plant` 11/12 (91.7%), `vampire_heart` 12/12 (100%), `corpse` 12/12 (100%), `time` 12/12 (100%), `stone_heart` 9/12 (75%, closest to the band — the one Core with no passive numbers of its own). Filed as **p10r**. |

**19 of 23 gates fully green.** G8, G13 (partial) and G23 are red/partial,
all three measured honestly with real numbers recorded (`.skip`-ed per class/
Core/tower rather than forced), matching CLAUDE.md's measurement rules. This
is a substantial improvement over the prior regeneration's "14 of 23" —
G1, G13's cap clause, G14, G19 and G22 all closed since `cc4ee58` — but G13's
overall gate status moved from a false "green in full" to an honest
"partial" this session, since its solo-viability clause was never actually
re-verified after `p10c`/`p10j`'s tuning passes moved the underlying data.

### The over-ceiling inversion (the current root cause behind G8 and G23)
`data/waves.json`'s `p10j`-`p10l` pacing pass (closing G1/G13) also closed
the old TD-wave-11-to-17 wall that used to sink G8, G14 and G23 alike — but
it overshot. G14 landed cleanly inside its band (18/20, 90%); G8 and G23
landed *past* their 70% ceilings instead of inside their [35%, 70%] bands,
for 9 of 11 classes and 4 of 5 Cores respectively. Only `necromancer` (G8,
4/12, a different early-death cause) and `stone_heart` (G23, 9/12, closest
to the band, the one Core with no passive of its own) still miss on the low
side. This is now a single retuning problem, not eleven-plus-five
independent balance stories — filed as **p10r**, scoped to avoid reopening
G1/G13.

### G13's newly-disclosed solo-viability drift (b072)
Independent of the over-ceiling story above: `tests/a4-single-type.test.ts`
(G13's other clause) has 4 live, non-`.skip`, failing assertions at HEAD —
`ember_brazier`/`tesla_coil` no longer clear T1 on all 5 seeds (3/5, 2/5),
and `mortar`/`venom_spore` now clear one T3 seed each instead of zero. First
directly confirmed by `b071` while fixing the unrelated 35%-share cap
(`git stash` isolation showed the fix touches neither), but never converted
into its own backlog item until this regeneration. See BACKLOG b072.

### Sweep table (`npx tsx tools/handoff-metrics.ts`, seeds 1–8, engineer, `cycles: 6`)

| Policy | Tier | Win rate | Median total | Median Act II survival | Median waves | Median level |
|---|---|---|---|---|---|---|
| maxbuild | 1 | 50% | 35.61 min | 641 s | 18 | 32 |
| maxbuild | 3 | 38% | 37.49 min | 652 s | 18 | 32 |
| maxbuild | 5 | 0% | 30.96 min | 449 s | 18 | 27 |
| hybrid | 1 | **88%** | 35.26 min | 613 s | 18 | 34 |
| hybrid | 3 | 75% | 38.49 min | 643 s | 18 | 34 |
| hybrid | 5 | 50% | 35.15 min | 659 s | 18 | 36 |
| turtle | 1 | 88% | 36.91 min | 629 s | 18 | 18 |
| no-move | 1 | 75% | 35.85 min | 630 s | 18 | 14 |
| greedy | 1 | 38% | 36.23 min | 644 s | 18 | 30 |
| greedless | 1 | 25% | 35.76 min | 638 s | 18 | 30 |
| kite | 1 | 0% | 2.81 min | 30 s | 3 | 3 |
| rush | 1 | 0% | 2.83 min | 33 s | 3 | 2 |
| walloff | 1 | 0% | 3.43 min | 23 s | 3 | 3 |

Median victorious run across the pool: **35.92 min** (29 wins).

**Live issue, unresolved (BACKLOG p10p)**: `maxbuild` has recovered
substantially since the prior regeneration (0%→50% T1, purely from the
`p10j`-`p10l` balance pass, no bot-code change) and `greedy`/`greedless` now
win 38%/25% (were both 0%), but `kite`, `rush` and `walloff` are still flat
at 0% across every seed — they predate the class/Core/VS-inheritance system
and were never retuned against it. `hybrid` and `turtle` remain the two most
reliable "plays and sometimes loses" arms.

**Live issue, unresolved (BACKLOG p10q)**: `no-move` (never repositioning)
still wins **75%** of the time at T1 — a genuine finding, not a fluke of one
sweep: it read 75% at `p10i`'s regeneration, moved to 100% in a fresh sweep
just before `p10m`/`p10n` (per BACKLOG b044's entry), and is back to 75%
here after `b071`'s `frost_obelisk` damage cut — consistent with VS combat
being survivable on tower-wielded damage alone at T1 and sensitive to the
exact per-tower damage tuning, not yet checked at T3/T5.

### VS damage share (`npx tsx tools/a5probe.ts`, top-10 winning-build pool)
| Tower | Share |
|---|---|
| frost_obelisk | 25.9% (was 42.7% pre-`p10j`/`p10c`, now well under the 35% cap) |
| mortar | 20.8% |
| ballista | 20.4% |
| ember_brazier | 19.1% |
| arrow_spire | 7.6% |
| venom_spore | 2.8% |
| tesla_coil | 1.6% |

`beacon_totem`/`harvest_sprout` contribute ~0% here — no top-10 build leans
on them as a primary damage source.

### Solo-tower viability (`npx tsx tools/a4probe.ts`, G13's a4 half)
| Tower | T1 clears | T3 clears |
|---|---|---|
| arrow_spire | 5/5 | 0/5 |
| ballista | 5/5 | 0/5 |
| ember_brazier | **3/5** | 0/5 |
| frost_obelisk | 5/5 | 0/5 |
| tesla_coil | **2/5** | 0/5 |
| mortar | 5/5 | **1/5** |
| venom_spore | 5/5 | **1/5** |
| palisade (no attack) | 0/5 | 0/5 |

The gate wants every attacking tower at 5/5 T1 / 0/5 T3. Four rows (bolded)
miss — see the b072 writeup above.

### Boon picks (`tools/handoff-metrics.ts`, 72-run pool, bot-driven — offer RNG, not player preference)
Range 79% (112 ranks / 50 runs) · Max HP 73% (118/46) · Attack 71% (126/45) ·
Defense 71% (88/45) · Area 67% (85/42) · Move 57% (75/36) · Attack Speed 54%
(66/34). Appearance rate spans 54–79% — no boon is starved or dominant in the
offer pool itself.

---

## 5. Known issues

### Skipped tests (the honest-red gate measurements)
`tests/p6e-class-diversity.test.ts` (G8, all 12 classes + diversity, `.skip`),
`tests/p-core-f-gates.test.ts` (G23, all 5 Cores, `.skip`), and
`tests/a4-single-type.test.ts` (G13's viability clause — **not** `.skip`-ed;
4 assertions genuinely fail, see b072) carry the live honest-red state.
`tests/p10d-run-length.test.ts`, `tests/p10c-weapon-share.test.ts` and
`tests/boss.test.ts` are un-skipped and green (G1/G13's cap/G14). None of the
skipped numbers is guessed or stale — each was re-measured this session or at
the P10 item that last touched it.

### Live issues
- **G8 and G23 over-shot from under-the-floor to over-the-ceiling** in the
  same pacing pass that closed G1/G13/G14 (§4 above) — a single retuning
  problem now, filed as **p10r**.
- **G13's solo-viability clause has a real, previously-unfiled 4-tower
  regression** (§4 above) — filed this session as **b072**.
- **`tools/gate-audit.ts`'s own coverage map is stale** for G8 and G15 (§4
  above) — filed as **p10o**, not yet fixed.
- **`no-move` wins 75% of the time at T1** without the character ever
  moving, and that number has shifted with unrelated balance tuning across
  three separate measurements (75%→100%→75%) — worth checking at T3/T5.
  Filed as **p10q**.
- **`kite`/`rush`/`walloff` still read 0% at T1** — filed as **p10p**;
  `maxbuild`/`greedy`/`greedless` have since recovered on their own from the
  same content-side pacing pass, no bot-code change needed for those three.
- **G8's diversity clause (2/12 distinct top-damage sources) has no item
  scoped to it** — `p10r` is explicitly win-rate-only; the diversity gap
  needs a kit/weapon-damage-ratio change, not a pacing retune, and nothing in
  the queue currently owns that.
- **Time Lord (the 12th class, added at `fb013`) has never had its own G8
  win-rate case written** — it's included in the diversity roster's
  12-class sweep but has no individual `it.skip('time_lord', ...)` band
  assertion the way the other 11 classes do.

---

## 6. Engineer's list: what is shallow or missing

Most consequential first, written against SPEC-FINAL's actual 1.0 bar
(content is complete — §2 — so everything below is a balance or engine-depth
question, not a scope gap).

1. **G8 and G23's over-ceiling inversion is the single most consequential
   open problem** (§4). Nine of eleven measured classes and four of five
   Cores now win 91.7–100% of the time — the same pacing pass that fixed
   G1/G13/G14 pushed both past their upper band instead of into it. `p10r`
   is filed and scoped to retune this without reopening G1/G13.

2. **G13's solo-viability clause quietly regressed and was never filed.**
   Four towers (`ember_brazier`, `tesla_coil`, `mortar`, `venom_spore`) no
   longer hit the exact T1/T3 numbers `tests/a4-single-type.test.ts` pins,
   confirmed pre-existing and unrelated to the fix that found it (`b071`).
   This regeneration filed it as **b072** — it sat undiscovered as a real
   failing (non-skipped) test for at least one prior session because the
   file is excluded from `test:fast` and no full-suite run happened to flag
   it as new.

3. **G8's diversity clause (2 of 12) is untouched by every pacing fix so
   far and has no owner.** Win rate and top-damage-source are independent
   axes here (`p10m`'s own finding) — closing G8's win-rate half via `p10r`
   will not move this at all. It needs a kit-damage-ratio or weapon-balance
   change, out of scope for any currently-queued item.

4. **`tools/gate-audit.ts`'s coverage map needs a re-derive** (§4, `p10o`,
   still open) — it still lists G8 and G15 as `hole` after both gained live
   coverage.

5. **The registered bot roster is half-refreshed.** `maxbuild`, `greedy`
   and `greedless` recovered on their own from the `p10j`-`p10l` pacing
   pass; `kite`, `rush` and `walloff` did not move at all and still read 0%
   — filed as **p10p**, unexecuted.

6. **`no-move`'s win rate is sensitive to balance tuning that has nothing to
   do with character movement** (75%→100%→75% across three measurements,
   tracking `frost_obelisk`'s damage value each time) — worth resolving
   explicitly whether this is expected (VS combat is tower-damage-dominated
   by design) or a design smell, filed as **p10q**.

7. **Time Lord, the 12th class, has no individual G8 win-rate measurement.**
   It rides along in the diversity roster's full 12-class sweep but nothing
   pins its own win-rate band the way the other 11 classes have.

**One structural note, unchanged from the v0.1 handoff and still true**: the
sim/render split and the data-driven content pipeline continue to hold up
well under a much larger content surface (12 classes, 10 towers, 5 Cores, 12
equipment items, 24 waves) — every number above came from `/data` or a live
measured test, not a remembered one. What moved this session is entirely
balance-band tuning (over- vs under-shooting the same gates) and one
previously-undisclosed regression, not the architecture.
